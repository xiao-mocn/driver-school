<coach-user id="coachUser" wx:if="{{ loginType === 'coach' }}"></coach-user>
<student-user id="studentUser" wx:if="{{ loginType === 'student' }}"></student-user>
<boss-user id="bossUser" wx:if="{{ loginType === 'boss' }}"></boss-user>